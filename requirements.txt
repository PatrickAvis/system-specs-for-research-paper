# No external dependencies required.
# The script uses only the Python standard library (subprocess).
# PyTorch detection is done via system call — install it separately on the target server if needed.
